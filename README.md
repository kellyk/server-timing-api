# server-timing-api

Proof of concept for setting service timing headers

To use:

```
npm install
npm run dev
```

* Visit http://localhost:3000
* Open console to see output, or run

```
window.performance.getEntriesByType('resource')
```
